Text field. `box` (default, light surfaces) or `line` — the underlined uppercase-label style the app's dark sign-in uses.
```jsx
<Input label="Phone number" placeholder="+20 100 000 0000"/>
<Input variant="line" label="Password" type="password"/>
```